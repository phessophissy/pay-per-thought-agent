# Pay-Per-Thought CRE Workflow

This directory contains the Chainlink CRE workflow used in the hackathon demo.

- This is a **Chainlink CRE workflow** defined in `workflow.yaml`.
- It executes the agent's reasoning steps in a deterministic sequence.
- Each step triggers **on-chain payment authorization** before execution.

Primary file:
- `workflow.yaml` — canonical workflow definition for judging.
